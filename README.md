# AIBV revision

1. Add tracking on the quiz to give feedback on areas of weekness and present questions that need attention
2. Add upload data so that the site can be public but the user loads up the JSON to prevent issues with AIBV

## Running a build

```sh
JSON_PASSPHRASE="your-passcode" node scripts/encrypt-json.mjs
```
